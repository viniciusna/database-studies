-- Inserting data

-- INSERT INTO [nome da tabela] ([coluna1], [coluna2], ...)
-- VALUES ([valor da coluna 1], [valor da coluna 2], ...),
-- ([valor da coluna 1], [valor da coluna 2], ...),
-- ...;

--#############################################################################################################################

-- Updating an column

-- UPDATE [nome da tabela]
-- SET [coluna a ser atualizada] = [novo valor da coluna], [outra coluna a ser atualizada] = [novo valor da coluna], ...
-- WHERE [coluna para achar o registro que vc quer] = [valor da coluna do dado que vc quer atualizar]

--#############################################################################################################################

-- Deleting an data

-- DELETE FROM [nome da tabela] WHERE [coluna para achar o registro que quer apagar] = [valor dessa coluna], ...;

